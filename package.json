{
  "name": "cc-sync-notes",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first Android notes prototype with optional WebDAV and GitHub sync.",
  "scripts": {
    "serve": "npx http-server www -p 4173",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "http-server": "^14.1.1"
  }
}
